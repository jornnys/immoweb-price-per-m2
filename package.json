{
  "name": "immoweb-price-per-m2",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension that displays price per square meter on Immoweb.be listings",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.1.1"
  }
}
